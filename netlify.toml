[build]
  publish = "."
  functions = "netlify/functions"

# Cualquier URL de la web antigua que ya no existe se manda a la portada
# con un 301, para no perder posicionamiento ni dejar errores 404 en Google.
[[redirects]]
  from = "/*"
  to = "/"
  status = 301
  force = false
